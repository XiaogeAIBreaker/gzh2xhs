# 架构重构设计文档

## 🎯 重构目标

基于现有 NestJS + TypeScript 架构，实现全面模块化重构，打造企业级可维护、高性能 Node.js 应用。

## 📋 架构概览

```
┌─────────────────────────────────────────────────────────────┐
│                    前端层 (Next.js 14)                      │
├─────────────────────────────────────────────────────────────┤
│                   API 网关层 (NestJS)                       │
│  ┌─────────────┬─────────────┬─────────────┬─────────────┐  │
│  │   认证模块   │   生成模块   │   财务模块   │   数据模块   │  │
│  └─────────────┴─────────────┴─────────────┴─────────────┘  │
├─────────────────────────────────────────────────────────────┤
│                  业务逻辑层 (Application)                    │
│  ┌─────────────┬─────────────┬─────────────┬─────────────┐  │
│  │   UseCase   │   UseCase   │   UseCase   │   UseCase   │  │
│  └─────────────┴─────────────┴─────────────┴─────────────┘  │
├─────────────────────────────────────────────────────────────┤
│                  领域层 (Domain)                            │
│  ┌─────────────┬─────────────┬─────────────┬─────────────┐  │
│  │    实体     │    服务     │    仓储     │   规则引擎   │  │
│  └─────────────┴─────────────┴─────────────┴─────────────┘  │
├─────────────────────────────────────────────────────────────┤
│                基础设施层 (Infrastructure)                   │
│  ┌─────────────┬─────────────┬─────────────┬─────────────┐  │
│  │   数据访问   │   外部API   │   缓存层    │   消息队列   │  │
│  └─────────────┴─────────────┴─────────────┴─────────────┘  │
├─────────────────────────────────────────────────────────────┤
│                    共享组件层                               │
│  ┌─────────────┬─────────────┬─────────────┬─────────────┐  │
│  │   配置中心   │   日志系统   │   安全组件   │   工具库    │  │
│  └─────────────┴─────────────┴─────────────┴─────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

## 🏗️ 目录结构

```
apps/api/
├── src/
│   ├── interfaces/              # 接口层
│   │   ├── http/               # HTTP 控制器
│   │   │   ├── controllers/    # REST API 控制器
│   │   │   ├── dto/           # 数据传输对象
│   │   │   ├── guards/        # 路由守卫
│   │   │   ├── interceptors/  # 拦截器
│   │   │   └── filters/       # 过滤器
│   │   ├── websocket/         # WebSocket 处理
│   │   └── grpc/             # gRPC 服务
│   ├── application/           # 应用层 (用例编排)
│   │   ├── usecases/         # 用例实现
│   │   ├── services/         # 应用服务
│   │   └── facades/          # 外观模式
│   ├── domain/               # 领域层
│   │   ├── entities/         # 领域实体
│   │   ├── value-objects/    # 值对象
│   │   ├── repositories/     # 仓储接口
│   │   ├── services/         # 领域服务
│   │   └── factories/        # 工厂模式
│   ├── infrastructure/       # 基础设施层
│   │   ├── database/         # 数据库访问
│   │   │   ├── repositories/ # 仓储实现
│   │   │   ├── migrations/   # 数据库迁移
│   │   │   └── seeds/        # 数据填充
│   │   ├── external/         # 外部服务集成
│   │   │   ├── ai-providers/ # AI 服务提供商
│   │   │   ├── cache/        # 缓存实现
│   │   │   └── messaging/    # 消息队列
│   │   ├── security/         # 安全组件
│   │   └── monitoring/       # 监控组件
│   ├── shared/               # 共享组件
│   │   ├── config/          # 配置管理
│   │   ├── constants/       # 常量定义
│   │   ├── types/           # 类型定义
│   │   ├── utils/           # 工具函数
│   │   ├── decorators/      # 自定义装饰器
│   │   └── validators/      # 验证器
│   ├── main.ts              # 应用入口
│   └── app.module.ts        # 根模块
├── test/                    # 测试文件
│   ├── unit/               # 单元测试
│   ├── integration/        # 集成测试
│   ├── e2e/               # 端到端测试
│   └── fixtures/          # 测试数据
├── docs/                   # 文档
│   ├── api/               # API 文档
│   ├── architecture/      # 架构文档
│   └── deployment/        # 部署文档
├── scripts/               # 构建和部署脚本
└── config/                # 配置文件
```

## 🔧 技术栈

### 核心框架
- **NestJS 10.4+**: 企业级 Node.js 框架
- **TypeScript 5.6+**: 静态类型检查
- **Node.js 18+**: 运行时环境

### 数据存储
- **TypeORM 0.3+**: ORM 框架
- **PostgreSQL 14+**: 主数据库
- **Redis 7+**: 缓存和会话存储
- **SQLite**: 测试数据库

### 消息队列
- **Bull Queue**: 任务队列
- **Redis**: 消息代理

### 安全
- **Passport.js**: 身份认证
- **JWT**: 无状态认证
- **bcrypt**: 密码加密
- **Helmet**: 安全头设置
- **CORS**: 跨域配置

### 监控与日志
- **Winston**: 日志框架
- **Prometheus**: 指标收集
- **Grafana**: 可视化面板

### API 文档
- **Swagger/OpenAPI 3.0**: API 文档生成
- **Postman**: API 测试

### 测试
- **Vitest**: 单元测试框架
- **Jest**: 集成测试框架
- **Supertest**: HTTP 测试
- **Playwright**: E2E 测试

## 🎨 设计模式

### 1. 依赖注入 (DI)
```typescript
@Injectable()
export class GenerateService {
  constructor(
    private readonly aiProvider: AiProvider,
    private readonly cache: CacheService,
    @Inject('AI_SERVICE') private readonly aiService: AiService,
  ) {}
}
```

### 2. 仓储模式 (Repository)
```typescript
export interface UserRepository {
  findById(id: string): Promise<User | null>;
  save(user: User): Promise<User>;
  delete(id: string): Promise<void>;
}
```

### 3. 工厂模式 (Factory)
```typescript
export class AiProviderFactory {
  static create(type: AiProviderType): AiProvider {
    switch (type) {
      case AiProviderType.DEEPSEEK:
        return new DeepSeekService();
      case AiProviderType.NANOBANANA:
        return new NanoBananaService();
      default:
        throw new Error(`Unknown AI provider: ${type}`);
    }
  }
}
```

### 4. 策略模式 (Strategy)
```typescript
export interface PricingStrategy {
  calculate(price: number, user: User): number;
}

export class PremiumPricingStrategy implements PricingStrategy {
  calculate(price: number, user: User): number {
    return price * 0.9; // 10% 折扣
  }
}
```

## 🔐 安全架构

### 认证与授权
- JWT Token 认证
- RBAC 角色权限控制
- API 限流与防护
- CSRF 保护

### 数据安全
- 输入验证与过滤
- SQL 注入防护
- XSS 攻击防护
- 敏感数据加密

### 供应链安全
- 依赖漏洞扫描
- 镜像安全扫描
- 代码安全静态分析
- 容器镜像签名

## 📊 性能优化

### 缓存策略
- 多级缓存架构
- 缓存预热与刷新
- 缓存穿透防护
- 分布式缓存

### 数据库优化
- 连接池管理
- 查询优化
- 索引策略
- 分库分表

### 异步处理
- 消息队列解耦
- 批处理优化
- 流式处理
- 并发控制

## 🚀 部署架构

### 容器化
- Docker 镜像优化
- 多阶段构建
- 镜像扫描
- 安全基线

### 编排管理
- Kubernetes 部署
- Helm Charts
- 自动扩缩容
- 滚动更新

### 监控运维
- 健康检查
- 指标监控
- 日志聚合
- 告警通知

## 📈 扩展性设计

### 水平扩展
- 无状态服务设计
- 负载均衡
- 数据分片
- 微服务架构

### 垂直扩展
- 性能调优
- 资源优化
- 算法优化
- 架构升级

## 🔄 CI/CD 流水线

### 代码质量
- 代码静态分析
- 安全漏洞扫描
- 依赖健康检查
- 测试覆盖率门禁

### 自动化测试
- 单元测试
- 集成测试
- 端到端测试
- 性能测试

### 持续集成
- 自动化构建
- 自动化部署
- 蓝绿部署
- 灰度发布

## 📝 开发规范

### 代码规范
- ESLint 规则配置
- Prettier 格式化
- TypeScript 严格模式
- 命名约定

### Git 规范
- 提交信息规范
- 分支管理策略
- 代码审查流程
- 版本发布流程

### 文档规范
- API 文档生成
- 架构文档维护
- 开发指南编写
- 部署文档更新

## 🎯 质量门禁

### 静态代码分析
- 代码复杂度检查
- 重复代码检测
- 安全漏洞扫描
- 依赖漏洞扫描

### 测试覆盖率
- 单元测试覆盖率 > 80%
- 集成测试覆盖率 > 70%
- 端到端测试覆盖率 > 60%
- 性能测试通过率 > 95%

### 性能指标
- API 响应时间 p95 < 200ms
- 数据库查询时间 p95 < 50ms
- 内存使用率 < 80%
- CPU 使用率 < 70%

## 📋 迁移策略

### 阶段一：标准化 (当前)
- 统一代码规范
- 完善测试覆盖率
- 优化项目结构
- 增强文档

### 阶段二：模块化重构
- 业务逻辑拆分
- 依赖关系优化
- 接口标准化
- 性能优化

### 阶段三：性能提升
- 缓存优化
- 数据库优化
- 异步处理优化
- 监控完善

### 阶段四：生产就绪
- 安全加固
- 部署优化
- 运维自动化
- 文档完善

## 🔍 关键决策记录

### ADR-001: 选择 NestJS 作为主框架
- **决策**: 使用 NestJS 替代原生 Express
- **原因**: 更好的类型支持、装饰器模式、依赖注入
- **影响**: 需要学习曲线，但长期收益显著

### ADR-002: 采用分层架构
- **决策**: 实施 DDD 分层架构
- **原因**: 提高代码可维护性、测试性、可扩展性
- **影响**: 初期开发成本增加，但长期维护成本降低

### ADR-003: 统一测试框架
- **决策**: 使用 Vitest + Jest 组合
- **原因**: Vitest 快速、Jest 成熟稳定
- **影响**: 测试开发效率提升

### ADR-004: API 文档生成策略
- **决策**: 使用 Swagger/OpenAPI 3.0
- **原因**: 标准化、可视化、支持代码生成
- **影响**: API 设计更加规范

## 🎯 成功指标

### 技术指标
- [ ] 测试覆盖率 ≥ 80%
- [ ] 代码复杂度 ≤ 10
- [ ] 技术债务 ≤ 5%
- [ ] 安全漏洞 = 0

### 性能指标
- [ ] API 响应时间 p95 ≤ 200ms
- [ ] 数据库查询时间 p95 ≤ 50ms
- [ ] 系统可用性 ≥ 99.9%
- [ ] 错误率 ≤ 0.1%

### 业务指标
- [ ] 开发效率提升 30%
- [ ] 缺陷率降低 50%
- [ ] 新功能交付周期缩短 40%
- [ ] 维护成本降低 60%

---

*本文档将持续更新，反映最新架构决策和实现状态*