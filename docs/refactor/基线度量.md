# 基线度量（M0）

## 测试覆盖率（Vitest v8）

- 语句：96.38%
- 分支：84.71%
- 函数：87.61%
- 行：96.38%

结论：覆盖率达到并略高于既定阈值（lines/statements≥95、branches≥84、functions≥85），维持良好质量基线。

## 构建状态（Next.js 生产构建）

- 结果：构建失败（模块缺失）。
- 报错：`src/app/api-docs/page.tsx` 依赖缺失 `swagger-ui-react` 与 `swagger-ui-react/swagger-ui.css`。
- 建议：
    - 安装缺失依赖或改造 API 文档页面为可选加载。
    - 在 CI 增加构建检查与依赖审计，避免此类问题进入主干。

## 性能/容量基线（待补充）

- 前端：Lighthouse（待运行）；关键路由交互延迟与渲染时序（待采集）。
- 服务端：核心 API 路由 P95/P99 延迟与错误率（待压测与采集）。
- 资源：CPU/内存使用（待压测与采集）。

## 后续动作

- 修复构建缺失依赖后，补充 Lighthouse 与压测数据。
- 建立度量看板与报警规则，纳入 CI 阶段与发布门槛。
